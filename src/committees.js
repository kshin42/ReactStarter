var React = require('react');

var CommitteesPage = React.createClass ({
  render: function () {
    return <h2 className="ui inverted header"> CommitteesPage </h2>
  }
});

module.exports = CommitteesPage;
